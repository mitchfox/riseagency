

This is a large multi-area request. Let me explore the relevant files quickly to scope the work, then present a focused plan.
<lov-tool-use id="toolu_vrtx_01GtH2vP8GPRhV1cfG9o7rHh" name="code--list_dir" integration-id="code" data="{\"dir_path\": \"src/pages\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01EVvMmat7EEEphR1hNG2GE9" name="code--search_files" integration-id="code" data="{\"query\": \"request-representation\", \"include_patterns\": \"*.tsx,*.ts\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01RjN9CHwc7mNT2uahTQEzQE" name="code--search_files" integration-id="code" data="{\"query\": \"staff_activity_log\", \"include_patterns\": \"*.ts,*.tsx\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01AY1DT6TxtsVaDc2C2ubUHP" name="code--search_files" integration-id="code" data="{\"query\": \"leaderboard\", \"include_patterns\": \"*.tsx,*.ts\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01EJEUHLHU5GkoDfLCh8YkKL" name="code--search_files" integration-id="code" data="{\"query\": \"RequestRepresentation\", \"include_patterns\": \"*.tsx\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01UehZ37vamyVFg1re8NzzWR" name="code--search_files" integration-id="code" data="{\"query\": \"leaderboard\", \"include_patterns\": \"*.tsx,*.ts\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_016H2HheaiPcdraRvcgFMmoC" name="code--search_files" integration-id="code" data="{\"query\": \"staff_activity_log\", \"include_patterns\": \"*.tsx,*.ts\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01LTMFC24mdiiqPdo1H9kscD" name="code--search_files" integration-id="code" data="{\"query\": \"Fuel For Football\", \"include_patterns\": \"*.tsx,*.ts\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_014bLPra4ADV6T6Cp5nsKVbD" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"staff_notification_events\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01X6tViDEin3vUm7yDDy1cMe" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"PWA.*last|lastRoute|last_route\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01WHGpNTCXPSiUiCn5cjVC1g" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"\\\"Other\\\"|category.*other\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_0199rkyd9pVbgv34voyDaH58" name="code--view" integration-id="code" data="{\"file_path\": \"src/components/staff/StaffAccountabilityOverview.tsx\", \"lines\": \"1-150\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01Nx51UjeZcaMpWkApNMhWKm" name="code--view" integration-id="code" data="{\"file_path\": \"src/components/staff/StaffNotificationsDropdown.tsx\", \"lines\": \"1-200\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01GroMFz4xSkH9ZdGjjB2nR7" name="code--view" integration-id="code" data="{\"file_path\": \"src/pages/RequestRepresentation.tsx\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_0139cUXV3pa4pna7RmJ8v2Zg" name="code--view" integration-id="code" data="{\"file_path\": \"src/main.tsx\", \"lines\": \"1-80\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01HuvpzcrQxGguLmoXYdRx7F" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"RepresentationDialog\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01CgYr1DfVc7i3PjhL4fo9F4" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"player_status.*fuel_for_football|fuel_for_football.*package\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_017bfY4pok6Q1P9gyH23bHUf" name="code--view" integration-id="code" data="{\"file_path\": \"src/components/RepresentationDialog.tsx\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01E6dhXE7m3znAn8pCdfr6g2" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.ts,*.tsx,*.sql\", \"query\": \"fff_package|fuel_package|package.*progress\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01HgkDVgdKTdVGTDmGr15rwT" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.ts,*.tsx\", \"query\": \"marketing-schedule|MarketingSchedule|schedule.*board.*kanban\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_019dkpfGrJUqMcfedywnPCNf" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx\", \"query\": \"Marketing.*Schedule|ScheduleBoard\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01ULEszviV9qAsNr6E3xNkCv" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"portal.*hub|hub.*weekly|fixtures.*upcoming.*hub\"}">
</lov-tool-use>
I have enough context. Let me look at the analysis editor for performance reports header and check the existing Cristiano Ronaldo report linking.
<lov-tool-use id="toolu_vrtx_01PaDfAF4iwXV11NtnMXKD3i" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"Cristiano|Ronaldo\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_019GHQKiRFdpULP1hGFDQdSa" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx\", \"query\": \"AnalysisManagement.*header|analysis.*editor.*top\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_012gpk1aRQoErscEpe7v67oB" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx\", \"query\": \"Spell Check|spellCheck=\\\\{true\\\\}|spellCheck=\\\\\\\"true\\\\\\\"\"}">
</lov-tool-use>

<lov-tool-use id="toolu_vrtx_01Gy9MVBno3XDqdX7nM2i7BZ" name="code--search_files" integration-id="code" data="{\"include_patterns\": \"*.tsx,*.ts\", \"query\": \"Schedule.*title.*Recurring|RecurringTask|recurringTasks\"}">
</lov-tool-use>
I have enough context to plan. Let me present.

<lov-plan>

## Plan: Multi-area improvements

### 1. Staff notifications — task detail and rename "Other"
- In `StaffAccountabilityOverview.tsx` improve the body text on every notification insert (`task_completed`, `Schedule Item Completed`, `task_reminder`) to always include who, exact task title and category. Already partly there for the active member, but also fire one for any staff who marks their own assigned task done, not just the currently viewed member. Audit all task/action update paths in this file so every status change emits a notification.
- In `StaffNotificationsDropdown.tsx` audit `CATEGORY_CONFIG` and add labels for known event types currently falling into the `Other` fallback (e.g. `error_report`, `comparison_request` already present, plus `task_reminder`, `Schedule Item Completed`, performance/club submissions, contract events). For anything that genuinely cannot be categorised, derive a label from the event type itself rather than hardcoded `Other`.

### 2. Leaderboard — capture every action
- `StaffAccountabilityOverview.tsx` leaderboard counts only items in `staff_tasks`. Update aggregation so it pulls completion counts from:
  - `staff_tasks.completion_log`
  - `marketing_schedule_items` posted-by + posted-at
  - `staff_activity_log` (created/updated/deleted entities by user)
- Group by user_id and timeframe (week/month/year) so logging anything anywhere increases their leaderboard score.

### 3. PWA — open on last visited page + Home button
- In `main.tsx` PWA scope guard, change behaviour from "force /portal or /staff" to: on PWA cold-start (only when path is `/` or root with no in-scope match) redirect to last saved route. Keep current scope persistence.
- Add a small "Home" icon button in both the staff header (`Header` area in `Staff.tsx`) and player portal header (in `Dashboard.tsx`/`PortalHeader`) that links to `/` so users can still get to the marketing landing page from inside the PWA.

### 4. Notification categories renamed
- Done in step 1 above — extend `CATEGORY_CONFIG` so everything has a meaningful label.

### 5. My Tasks — visual schedule + upcoming personal slot
- Replace the current `renderScheduleTab` in `StaffAccountabilityOverview.tsx` with a layout mirroring `ScheduleManager` (the marketing & brand weekly board): 7 day columns, drag-drop, image thumbnails, post-type chips, status pills.
- Above the tasks list (always visible regardless of active tab), add a new "This week" strip showing:
  - Upcoming fixtures from `fixtures` (next 7 days)
  - Tasks/schedule items due this week assigned to the active staff member
- Compact horizontally scrollable strip styled like portal `Hub`.

### 6. Request Representation — desktop/tablet, FAQ, form fixes, navigation, hero report link
- File: `src/pages/RequestRepresentation.tsx`.
  - Add desktop/tablet (md+) layout branch: wide hero, two-column card grid, larger typography. Keep mobile branch untouched.
  - Add header buffer (top safe spacing) and footer buffer so first/last cards aren't clipped behind native chrome.
  - Add small subtitle under each section card (above the title, below the icon) using a short "what this section explains" line; reuse existing `eyebrow` content.
  - Inside the card detail view add Prev / Next buttons cycling through `CARD_META`.
  - Add a new FAQs section per age group (under-18 vs over-18) with collapsible questions covering scouting, fees, agreement, parental consent, timeline.
  - On the Performance card detail, add a button "See an example performance report" that opens the live Cristiano Ronaldo report (provide the existing public report URL — confirmed link slug to be set).
- File: `src/components/RepresentationDialog.tsx`.
  - Make dialog properly scrollable with `max-h-[85vh] overflow-y-auto` and ensure inputs are reachable.
  - When age group is `under18`, conditionally require `parentName` and `parentPhone` fields. Pass `ageGroup` prop from `RequestRepresentation.tsx`.
  - Update zod schema to enforce parent fields when ageGroup is under-18.

### 7. Fuel for Football — package tracker on analysis & report editors
- Database (migration):
  - New table `fff_packages` (id, player_id, package_size int default 5, started_at, notes).
  - New table `fff_package_completions` (id, package_id, analysis_id nullable, performance_report_id nullable, fixture_id nullable, completed_at, completed_by).
  - RLS: staff can read/write.
- UI components:
  - New shared `FFFPackageHeader.tsx` showing current package, progress bar (`X / N completed`), list of which fixtures/games consumed, plus "Mark this game" and "Add new package" buttons.
  - Mount at top of `AnalysisManagement.tsx` editor and at top of `CreatePerformanceReportDialog.tsx` (or report edit view) only when `player.player_status === 'fuel_for_football'`.
  - Marking a game saves the analysis_id / report_id / fixture_id to `fff_package_completions` so future opens know the game already counted.

### Files to update
- `src/main.tsx`
- `src/pages/Staff.tsx` (Home button)
- `src/pages/Dashboard.tsx` (Home button)
- `src/components/staff/StaffAccountabilityOverview.tsx`
- `src/components/staff/StaffNotificationsDropdown.tsx`
- `src/pages/RequestRepresentation.tsx`
- `src/components/RepresentationDialog.tsx`
- `src/components/staff/AnalysisManagement.tsx`
- `src/components/staff/CreatePerformanceReportDialog.tsx`
- New: `src/components/staff/FFFPackageHeader.tsx`
- New migration for `fff_packages` + `fff_package_completions`

### Validation
- Notifications: complete a task, confirm staff/task name appear in the body, no `Other` category remains.
- Leaderboard: log an action elsewhere (e.g. update a player) and confirm count rises.
- PWA: kill app on `/staff?section=analysis`, relaunch, confirm it opens there. Tap Home in header, confirm `/` loads.
- My Tasks: confirm visual weekly board renders and upcoming-fixtures strip shows correct items.
- Request Representation: open on desktop and tablet, work through all sections with Prev/Next, submit form on under-18 path with parent fields enforced, click Ronaldo report link.
- Fuel for Football: open an analysis for an FFF player and confirm package header appears, mark a game, reopen and confirm it's counted.

